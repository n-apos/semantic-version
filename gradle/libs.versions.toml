[versions]

## Plugin ##
pluginPublish = "1.2.1"
version = "0.1.0"

[libraries]

[plugins]
plugin-publish = { id = "com.gradle.plugin-publish", version.ref = "pluginPublish" }
napos-version = { id = "com.n-apos.version", version.ref = "version" }